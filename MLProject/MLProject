name: heartdisease-ml-project

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      n_estimators:  {type: int,   default: 150}
      max_depth:     {type: int,   default: 4}
      learning_rate: {type: float, default: 0.1}
    command: >
      python modelling.py {n_estimators} {max_depth} {learning_rate}
